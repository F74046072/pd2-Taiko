#include "Judge.h"
#include "stdlib.h"
#include "time.h"
/*#include <QTimer>
#include <QGraphicsScene>
#include <QKeyEvent>

Judge::Judge(QGraphicsItem *parent) : QObject(),QGraphicsRectItem(parent){
    time=5;
    timer = new QTimer(this);
    connect(timer, SIGNAL(timeout()), this, SLOT(makedrum()));
    timer->start(100);

}

void Judge::makedrum(){
    Drum *drum = new drum();
}

void Judge::keyPressEvent(QKeyEvent *keyevent){
    if(pos().x()>50&&pos().x()<100){
        if(mode==0&&keyevent->key()==Qt::Key_F){

            scene()->removeItem(this);
            delete this;
        }
        else if(mode==1&&keyevent->key()==Qt::Key_F){

            scene()->removeItem(this);
            delete this;
        }
    }
}
*/
